<section class="g-cta">
  <div class="container g-cta-inner">
    <span class="g-cta-float float-1" aria-hidden="true">Five day turnaround ✦</span>
    <span class="g-cta-float float-2" aria-hidden="true">No contracts ★</span>
    <h2 class="g-cta-title">Found the one?</h2>
    <p class="g-cta-sub">
      Tell us which template feels like you. We take it from there.
    </p>
    <a
      href="https://formrobin.com/f/344no93"
      target="_blank"
      rel="noopener"
      class="btn btn-ink g-cta-btn"
      >Schedule a call</a
    >
  </div>
</section>

<style>
  .g-cta {
    background: var(--cream-deep);
    border-top: 2px solid var(--ink);
    padding: 140px 0;
    overflow: hidden;
  }
  .g-cta-inner {
    position: relative;
    text-align: center;
  }
  .g-cta-title {
    font-family: "Unbounded", sans-serif;
    font-weight: 700;
    font-size: clamp(44px, 7vw, 92px);
    letter-spacing: -0.03em;
    line-height: 1;
    margin-bottom: 24px;
  }
  .g-cta-sub {
    font-size: 19px;
    font-weight: 500;
    color: #3a3a3a;
    margin-bottom: 44px;
  }
  .g-cta-btn {
    font-size: 17px;
    padding: 20px 38px;
  }
  .g-cta-float {
    position: absolute;
    background: #fff;
    border: 2px solid var(--ink);
    border-radius: 100px;
    padding: 10px 18px;
    font-weight: 700;
    font-size: 14px;
    box-shadow: 4px 4px 0 var(--ink);
    white-space: nowrap;
  }
  .float-1 {
    top: -44px;
    left: 6%;
    transform: rotate(-8deg);
    background: var(--green);
  }
  .float-2 {
    bottom: -30px;
    right: 8%;
    transform: rotate(6deg);
    background: var(--yellow);
  }
  @media (prefers-reduced-motion: no-preference) {
    .float-1 { animation: cta-bob 6s ease-in-out infinite; }
    .float-2 { animation: cta-bob 7s ease-in-out 1s infinite; }
    @keyframes cta-bob {
      0%, 100% { translate: 0 0; }
      50% { translate: 0 -8px; }
    }
  }
  @media (max-width: 900px) {
    .g-cta { padding: 88px 0; }
    .float-1 { top: -56px; left: 0; }
    .float-2 { bottom: -52px; right: 0; }
  }
</style>
