<header class="site-header">
  <div class="container header-inner">
    <a class="brand-mark" href="/" aria-label="InsurePages home">
      {/* Decorative: the anchor's aria-label carries the accessible name, so
          an alt here would only duplicate it. The lockup's "by S3 Labs" is
          carried as real text in the footer. */}
      <img
        class="brand-logo"
        src="/images/insurepages-logo.png"
        alt=""
        width="655"
        height="259"
      />
    </a>
    <nav class="primary-nav" aria-label="Main">
      <a class="sticker nav-link" href="/#build">Approach</a>
      <a class="sticker nav-link" href="/#method">Method</a>
      <a class="sticker nav-link" href="/templates/">Templates</a>
      <a class="sticker nav-link" href="/#pricing">Packages</a>
      <a class="sticker nav-cta" href="/#contact">Start now <span aria-hidden="true">✨</span></a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    background: var(--cream);
    z-index: 100;
    padding: 20px 0;
    border-bottom: 2px solid var(--ink);
  }
  .site-header > .container {
    max-width: 100%;
    padding-left: 24px;
    padding-right: 40px;
  }
  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
  }
  .brand-mark {
    display: flex;
    align-items: center;
    text-decoration: none;
    color: var(--ink);
    /* Flex items default to min-width:auto, which would let the logo's
       intrinsic width push the header past the viewport. */
    min-width: 0;
  }
  /* 205px keeps the header the same height it was with the old text pill
     (~123px), so the sticky header doesn't shift in-page anchor landings. */
  .brand-logo {
    display: block;
    width: 205px;
    max-width: 100%;
    height: auto;
  }
  .primary-nav {
    display: flex;
    gap: 10px;
    align-items: center;
  }
  .primary-nav a { text-decoration: none; color: var(--ink); }
  /* Spec fix 1a: centered label. Emoji sits in its own span so the text
     block itself stays optically centered inside the pill. */
  .nav-cta {
    background: var(--yellow);
    gap: 6px;
    justify-content: center;
  }
  @media (max-width: 900px) {
    .nav-link { display: none; }
  }
  /* Verification fix (Task 8): on narrow phones the brand mark plus the
     "Start now" sticker (white-space: nowrap) don't fit the content box and
     push the header past the viewport edge. Tighten the padding/gap to
     reclaim width. Budget at a 320px viewport: 292px of content box, minus
     the 121px sticker and the 8px gap, leaves 163px for the logo -- 152px
     keeps a margin. Contrast isn't a factor at this size: WCAG 1.4.3
     exempts text that is part of a logo. */
  @media (max-width: 480px) {
    .site-header > .container { padding-left: 14px; padding-right: 14px; }
    .header-inner { gap: 8px; }
    .brand-logo { width: 152px; }
    .nav-cta { padding: 10px 14px; }
  }
</style>
